import React from "react"
import { Link } from "gatsby"
import Layout from "../components/layout"

export default () => (
  <Layout>
    <h1>Thank you!</h1>
    <p>
      Check your inbox for the latest news about Gatsby (Not a real newsletter).
    </p>
    <Link to="/">Go back to home</Link>
  </Layout>
)
